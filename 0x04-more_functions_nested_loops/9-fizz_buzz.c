#include <stdio.h>

/**
 * main - entry for cod
 *
 * return: always 0
 */

int main(void)
{
int n;
n = 0;

while (n++ < 100)
	if (((n % 5) == 0) && ((n % 3) == 0))
		printf("FizzBuzz ");
	else if ((n % 3) == 0)
		else if ((num % 5) == 0
{
if (num != 100)
printf("Buzz ");

else
printf("Buzz");
}

else
printf("%d ", num);

printf("\n");
return (0);
}

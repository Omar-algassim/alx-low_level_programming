#include 'main.h'

/**
*-isupper checks for uppercase character
*written by omar
* Returns 1 if c is uppercase
*Returns 0 otherwise
*/

int _isupper(int c);
{
char u;

if (u <= 'a' && u >= 'z')

	return (1);
	else 		
		return (0);

}

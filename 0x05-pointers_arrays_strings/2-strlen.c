#include "main.h"

/**
 * _strlen - return the length of string
 * written by omar
 * @s: the vlaue of length
 * Return: the length of string
 */
int _strlen(char *s)
{
int i;

i = 0;
while (s[i] != '\0')
{
	i++
}
return (i);
}

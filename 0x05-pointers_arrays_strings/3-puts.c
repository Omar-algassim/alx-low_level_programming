#include "main.h"

/**
 * _puts - to print stdout
 * written by omar
 * @str: the string stdout
 * Return: alyaws 0
 */

void _puts(char *str)
{
char str[7] = "stdout";

printf("%s\n", str);

return (0);
}
